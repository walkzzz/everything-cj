# Everything-CJ 仓颉版 - 工具链架构设计

> **技能**: cangjie-tools  
> **版本**: v1.0  
> **日期**: 2026-04-03  
> **协作技能**: cangjie-stdx, cangjie-db, cangjie-net, cangjie-ui

---

## 一、项目概述

### 1.1 项目定位

Everything-CJ 是仓颉编程语言实现的高性能本地文件秒级搜索工具，基于 NTFS MFT 实现毫秒级文件名检索。

### 1.2 核心价值

```
┌─────────────────────────────────────────────────────────────────┐
│                    Everything-CJ 核心价值                       │
└─────────────────────────────────────────────────────────────────┘

✅ 基于 NTFS MFT 秒级索引（非遍历扫描）
✅ 搜索响应 <100ms，输入无卡顿
✅ 内存占用 <50MB（数十万文件）
✅ 本体 <500KB，轻量绿色
❌ 不搜索文件内容（仅文件名）
```

---

## 二、系统架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    Everything-CJ 系统架构                       │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                          表现层 (Presentation)                  │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │  主窗口     │  │  CLI 工具   │  │ HTTP 服务   │            │
│  │  (Win32)    │  │  ES.exe     │  │  (8080)     │            │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘            │
└─────────┼────────────────┼────────────────┼────────────────────┘
          │                │                │
          ▼                ▼                ▼
┌─────────────────────────────────────────────────────────────────┐
│                          业务层 (Business)                      │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    搜索引擎 (SearchEngine)                │   │
│  │  ┌───────────┐  ┌───────────┐  ┌───────────┐           │   │
│  │  │ 查询解析  │  │ 索引查询  │  │ 结果排序  │           │   │
│  │  └───────────┘  └───────────┘  └───────────┘           │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    索引引擎 (IndexEngine)                 │   │
│  │  ┌───────────┐  ┌───────────┐  ┌───────────┐           │   │
│  │  │ MFT 读取  │  │ 文件扫描  │  │ 实时监控  │           │   │
│  │  └───────────┘  └───────────┘  └───────────┘           │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                          数据层 (Data)                          │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │  索引数据库 │  │  配置存储   │  │  缓存系统   │            │
│  │  (SQLite)   │  │  (TOML)    │  │  (Memory)   │            │
│  └─────────────┘  └─────────────┘  └─────────────┘            │
└─────────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                          系统层 (System)                        │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │ 文件系统    │  │ 注册表      │  │ 网络 API    │            │
│  │ (NTFS/FAT)  │  │ (自启)      │  │ (WinHTTP)   │            │
│  └─────────────┘  └─────────────┘  └─────────────┘            │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 模块架构

```
Everything-CJ/
├── core/                          # 核心引擎
│   ├── mft_reader.cj              # MFT 读取器
│   ├── index_engine.cj            # 索引引擎
│   └── file_monitor.cj            # 文件监控
│
├── search/                        # 搜索模块
│   ├── query_parser.cj            # 查询解析器
│   ├── index_query.cj             # 索引查询
│   └── result_ranker.cj           # 结果排序
│
├── storage/                       # 存储模块
│   ├── index_db.cj                # 索引数据库
│   └── config.cj                  # 配置管理
│
├── network/                       # 网络模块
│   ├── http_server.cj             # HTTP 服务器
│   └── api.cj                     # API 接口
│
├── ui/                            # UI 模块
│   ├── main_window.cj             # 主窗口
│   ├── search_bar.cj              # 搜索栏
│   └── result_list.cj             # 结果列表
│
└── cli/                           # CLI 工具
    ├── es.cj                      # ES.exe 命令行
    └── main.cj                    # 入口
```

---

## 三、专家技能协作

### 3.1 主技能: cangjie-tools

| 职责 | 内容 |
|------|------|
| 工具链架构 | 整体架构设计、模块划分 |
| 构建系统 | cjpm 配置、打包发布 |
| CLI 工具 | ES.exe 命令行接口 |
| 测试框架 | 单元测试、集成测试 |

### 3.2 协作技能: cangjie-stdx

| 职责 | 内容 |
|------|------|
| 文件系统 | MFT 读取器、文件监控 |
| 网络模块 | HTTP 服务器、WebSocket |
| 配置管理 | TOML 配置解析 |

### 3.3 协作技能: cangjie-db

| 职责 | 内容 |
|------|------|
| 索引数据库 | SQLite Schema 设计 |
| 查询优化 | 索引查询优化 |
| 增量更新 | 增量索引更新机制 |

### 3.4 协作技能: cangjie-net

| 职责 | 内容 |
|------|------|
| HTTP API | RESTful API 设计 |
| 服务器 | HTTP 服务器实现 |

### 3.5 协作技能: cangjie-ui

| 职责 | 内容 |
|------|------|
| 主窗口 | Win32 窗口、消息循环 |
| 搜索栏 | 输入框、自动完成 |
| 结果列表 | 列表显示、右键菜单 |

---

## 四、开发路线图

| 阶段 | 时间 | 内容 | 技能 |
|------|------|------|------|
| Phase 1 | 2 周 | 核心引擎（MFT 读取、索引构建） | cangjie-stdx, cangjie-db |
| Phase 2 | 2 周 | 搜索模块（查询解析、结果排序） | cangjie-stdx |
| Phase 3 | 1 周 | CLI 工具 | cangjie-tools |
| Phase 4 | 2 周 | HTTP API | cangjie-net |
| Phase 5 | 2 周 | UI 界面 | cangjie-ui |
| Phase 6 | 1 周 | 测试与优化 | cangjie-tools |

---

## 五、性能指标

| 指标 | 目标值 |
|------|--------|
| 索引构建速度 | < 1秒 (10万文件) |
| 搜索响应时间 | < 100ms |
| 内存占用 | < 50MB (10万文件) |
| CPU 占用 | 空闲 < 1%，索引时 < 20% |
| 启动时间 | < 1秒 |
| 软件体积 | < 500KB |

---

> **文档版本**: v1.0  
> **技能**: cangjie-tools  
> **日期**: 2026-04-03
